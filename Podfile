# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DailyRate2' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Reactive
	pod 'RxSwift', '4.4.0'
	pod 'RxCocoa', '4.4.0'
	pod 'RSSwizzle', '0.1.0'
	pod 'Action', '3.9.1'
	pod 'RxSwiftExt', '3.4.0'

  target 'DailyRate2Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DailyRate2UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
